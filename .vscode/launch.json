{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Debug Emulator",
        "type": "go",
        "request": "launch",
        "mode": "auto",
        "program": "${workspaceFolder}/",
        "args": ["debug"],
        "cwd": "${workspaceFolder}"
      }
    ]
  }
  